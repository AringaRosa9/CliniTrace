# 临床数据结构化平台 PRD

版本：v0.1  
日期：2026-09-16  
状态：MVP 产品设计与交互原型；尚未接入生产后端、OCR 或模型。  
产品名：临床数据结构化平台（Clinical Data Structuring Platform）。

## 1. 背景与产品定位

面向医药企业研究院的数据生产环节，把门诊记录、住院记录、检验检查报告及扫描文档转化为高质量结构化数据。产品定位为「面向医药研究的病历结构化与审核工作台」，核心价值是提取结果可追溯、质量可衡量、错误可纠正。

业务链路：非结构化文书 → 文档解析 → 事实抽取 → 术语标准化 → 规则校验 → 人工复核 → 研究数据集。审核纠错经复核后进入候选训练与评测样本库，形成版本可控的数据改进闭环。

## 2. 用户与目标

| 角色 | 主要任务 | 产品价值 |
| --- | --- | --- |
| 数据运营人员 | 导入文档、查看任务、处理失败与关联问题 | 降低整理和重复录入成本 |
| 医学审核人员 | 对照原文、纠正字段、确认不确定项 | 降低复核时间，保留判断依据 |
| 研究人员 | 查询、筛选、导出经审核的数据 | 获得一致、可追溯的研究数据 |
| 算法／标注负责人 | 管理 Schema、指南、金标准与评测 | 量化错误与版本提升 |
| 项目管理员 | 管理权限、术语版本、留存与审计 | 控制数据使用范围与质量标准 |

第一版验收目标：上传一份门诊记录和一张检验报告，得到带原文证据的结构化事实，完成逐项复核，导出可追溯结果。正式业务试点以质量、人工审核耗时和单份成本衡量价值。

## 3. MVP 范围

### 3.1 首发场景

内分泌科／糖尿病研究：门诊记录与检验报告，围绕诊断、病史、用药、检验指标和时间信息设计。

| 维度 | P0：MVP | P1／后续 |
| --- | --- | --- |
| 输入 | 文本、电子 PDF、清晰扫描件／图片 | 手写体、复杂扫描表格、批量系统对接 |
| 文书 | 门诊记录、检验报告 | 住院长病历、多科室、多文书病程 |
| 输出 | 可编辑结构化表格、JSON、CSV | 按合作方约定的 FHIR 映射与 API |
| 抽取 | 单文书／同次就诊事实和证据 | 跨就诊关系与复杂时序 |
| 审核 | 缺失／冲突提示、逐项确认、审计 | 多级复核、自动通过策略 |
| 术语 | 小范围、版本固定的疾病与检验映射 | 多词库、MedDRA、企业映射字典 |

影像先支持影像报告文字结构化；CT／MRI 原始影像解释单独立项。首版不提供临床诊断或治疗决策。

### 3.2 原型边界

当前 HTML 是独立、无需构建的前端演示，包含合成病例。支持页面导航、字段与原文联动、字段编辑、问题处置、审核提交、JSON／CSV 下载、示例文档切换和本地文件预览。演示修改只保存在当前页面内存；刷新恢复示例。

本地导入仅用于预览，不向远程服务上传。真实文件不触发伪造的抽取结果；显示「尚未接入解析服务」。原型不宣称具有生产权限隔离、真实置信度、真实模型性能或后台持久化。

## 4. 用户流程与状态

1. 新建任务，选择文书与抽取模板，确认文档来源及处理权限。
2. 校验文件类型、大小、损坏、加密与重复情况。
3. 解析文字层；必要时执行 OCR、版面分析和表格重建。
4. 文书分类，按模板抽取，返回事实与证据。
5. 术语候选检索与消歧，执行规则校验。
6. 进入审核队列，逐项对照、修改或保留未知。
7. 所有阻断项被明确处置且审核人员确认完成复核后，提交审核。
8. 导出审核结果与版本信息；经二次复核的纠错进入改进样本池。

任务状态：已导入 → 排队中 → 解析中 → 抽取中 → 待审核 → 已审核。解析失败与抽取失败保留独立原因和可重试操作；退回进入待补充材料状态。审核后再次编辑，必须回到待审核。

## 5. 信息架构与页面

| 页面 | P0 内容 | 对应 JD |
| --- | --- | --- |
| 文档任务 | 导入、类型、处理阶段、失败原因、重试、患者／就诊关联 | 多模态文档处理 |
| 审核工作台 | 原文、字段、证据、异常、审核与修改历史 | 人机协同闭环 |
| 抽取模板 | 字段类型、重复结构、枚举、必填、证据要求、指南版本 | Schema 与标注规范 |
| 术语管理 | 原文术语、候选、编码体系、版本、映射状态 | 术语标准化 |
| 数据集 | 患者／就诊／文书筛选、审核状态、数据导出 | 结构化数据生产 |
| 质量评测 | 字段／span／关系／时间／映射质量，人工时间和成本 | 质量体系 |

## 6. 核心功能要求

### 6.1 文档任务

- 每个文件有唯一文档标识、文件哈希、来源、归属、文书类型、上传记录和运行记录。
- 使用可信患者／就诊标识关联，禁止仅凭同名自动合并。
- 支持进度、取消与明确失败原因，重试保持幂等。
- 长任务由异步 Worker 处理；大文件、加密文件、无法识别文件进入可解释失败状态。
- 原件、OCR 文字、版面坐标和模型输出分别保留版本。

### 6.2 抽取模板与标注指南

- 字段支持字符串、数值、日期、枚举、对象和数组，诊断／药物／检验为可重复记录。
- 定义字段语义、来源范围、正反例、嵌套实体、跨段关系和时间处理规则。
- 区分缺失、未提及、明确否认、不适用与无法辨认。
- 发布后的模板不可原位修改；创建新版本并关联评测。
- 金标准由标注、复核、分歧裁决形成；记录标注者、指南版本及裁决理由。

### 6.3 审核工作台

桌面布局采用三栏：左侧原始文档，中间结构化结果，右侧质量检查与审核记录。顶部固定显示病例／就诊上下文与文书标识。

- 点击结果定位相应文书、页码与原文高亮；点击证据反向选中字段。
- 编辑时展示原始值、当前值、来源和修改理由。
- 缺失剂量应保留未知；禁止根据常规用量补全。
- 无法确定编码时允许「保留待映射」，禁止模型自由编造编码。
- 审核问题可修正或带理由接受未知；已处理不等于已经补齐数据。
- 提交前展示未处理项；阻断项未处理时不能提交。
- 整份审核需明确确认已完成原文复核，不能以关闭异常提示替代事实审核。
- 审核后导出带状态的结果；再次编辑导致审核失效，并保留历史。

### 6.4 术语管理

ICD 面向疾病分类，需与机构实际采用的版本及本地扩展对齐。LOINC 面向检验／观察项目；需结合标本、方法、时间属性等信息消歧。MedDRA 后续用于药物安全场景，并确认使用许可。

处理流程：原始表述 → 词库检索 → 候选排序 → 上下文消歧 → 规则校验 → 映射／待映射。保存体系、版本、编码、显示名、匹配依据和人工确认信息。

### 6.5 数据集与导出

- 默认导出已审核记录；草稿必须显式标记，不能与已审核数据混淆。
- JSON 保留嵌套关系、证据、质量提示和版本；CSV 每行一条事实，包含患者／就诊／文书键。
- 防止 CSV 单元格公式注入，正确处理中文、引号、换行与逗号。
- 支持追溯到原文档；导出受权限与用途限制，记录操作审计。
- 对外交换时按约定映射 FHIR：Condition、Observation、DiagnosticReport 等。JSON 不等同于 FHIR 合规。

## 7. 数据模型

核心层次：Project → Patient → Encounter → Document → ExtractionRun → ClinicalFact → Evidence。审核事件 ReviewEvent 独立保存。事实可有多个证据，文档与运行采用不可变版本。

| 实体 | 关键字段 |
| --- | --- |
| Patient | patient_id、project_id、受控来源标识 |
| Encounter | encounter_id、patient_id、就诊类型、时间、科室 |
| Document | document_id、encounter_id、type、file_hash、source、version |
| ExtractionRun | run_id、document_version、schema_version、model_version、prompt_version、状态 |
| ClinicalFact | fact_id、类别、原始值、标准化值、否定／疑似、归属、事件时间、缺失原因 |
| Evidence | document_id、page、span、bbox、quote、OCR 版本 |
| Coding | system、version、code、display、候选与映射状态 |
| ReviewEvent | actor、时间、操作、前后值、理由、review_status |

时间需分别记录事件时间和文书时间。相对日期保留原文及推导依据，不把「5年」擅自变成精确诊断日期。跨文书冲突保留多条事实及来源，禁止静默覆盖。

### 合成示例

原文：「既往2型糖尿病5年，否认冠心病史。现服二甲双胍，剂量不详。2026年9月1日空腹血糖8.2 mmol/L。」

| 类别 | 结构化结果 | 语义 |
| --- | --- | --- |
| 诊断 | 2型糖尿病 | 既往病史、肯定 |
| 病程 | 5年 | 原文相对时长，非精确起病日期 |
| 病史 | 冠心病 | 明确否认，不能写入阳性诊断 |
| 用药 | 二甲双胍 | 当前使用 |
| 剂量 | null | explicitly_unknown，原文「剂量不详」 |
| 检验 | 空腹血糖，8.2 mmol/L | 检验日期2026-09-01 |

## 8. AI 与工程架构

### 8.1 处理管线

1. 电子 PDF 优先读取文字层；扫描件使用 OCR＋版面分析，保留坐标和阅读顺序。
2. 复杂表格／手写体可按质量信号转 VLM 或人工处理，不假设所有文档均可自动解析。
3. 按文书类型路由 Schema，按章节提取长文档，再在就诊范围内合并关系与时间。
4. LLM 输出受约束 JSON 和证据；服务端做类型、枚举、单位、日期与证据存在性检查。
5. 术语检索与消歧独立于自由生成，编码须来自指定版本词库。
6. 风险分层与人工审核；模型输出视为待验证事实。

先建立提示词＋受约束输出＋规则基线，根据稳定错误类型和足够标注数据决定是否微调。微调收益用独立测试集和成本比较评估。

### 8.2 建议技术栈

| 层 | 建议 |
| --- | --- |
| 前端 | React／Next.js；文档预览、证据定位、表格编辑 |
| 服务端 | Python FastAPI；任务、抽取、审核、导出 API |
| 异步任务 | Worker＋队列；重试、幂等、阶段进度 |
| 数据库 | PostgreSQL；业务实体、事实、证据和审计 |
| 文件存储 | 私有对象存储；原始文件和派生产物分别保存 |
| OCR／版面 | PaddleOCR PP-StructureV3 等候选，按医疗样本评测选择 |
| 模型层 | 可替换适配器，根据数据环境和样本评测选型 |
| 评测 | 离线脚本、冻结测试集、版本比较 |

候选 API：POST /documents、GET /jobs/:id、POST /documents/:id/extractions、GET /encounters/:id/facts、PATCH /facts/:id（含版本检查）、POST /reviews、POST /exports。正式实现补齐租户鉴权、并发冲突、幂等键与失败契约。

## 9. 质量评测与置信度

初期可准备100～300份覆盖常见及困难情形的文书作为工程验证起点，不能凭样本量宣称可投入生产。

| 指标 | 定义与用途 |
| --- | --- |
| 字段 P／R／F1 | 区分错误提取和遗漏；按关键字段及文书类型分层 |
| Span F1 | 严格／宽松匹配分别报告，检验证据定位 |
| 关系准确率 | 药名－剂量、检验－结果－单位配对是否正确 |
| 时间准确率 | 事件日期与记录日期是否混淆 |
| 术语质量 | 映射正确率、覆盖率、主动拒答比例 |
| 高风险错误 | 否定变肯定、剂量错误、患者串联等错误数 |
| 审核效率 | 中位与P95审核时间、每份修改数、退回比例 |
| 成本与稳定性 | 单份模型／OCR成本、处理时长、失败率 |

按患者隔离训练／开发／测试集；增加跨机构与模板外测试。测试集冻结，纠错不能直接回流污染测试集。

初期显示规则依据和复核等级，不把模型自报概率当可靠置信度。有足够独立标注数据后进行校准，再根据关键错误率确定自动通过阈值。高风险字段保留人工策略。

## 10. 数据保护与非功能要求

- 演示使用合成数据，正式接入先明确合法处理依据、用途与授权范围。
- 医疗健康信息属于敏感个人信息；去掉姓名不当然构成匿名化。
- 定义租户／项目隔离、角色权限、传输与静态加密、访问审计、导出控制、删除与留存期限。
- 明确模型服务商是否留存或训练、部署地域、跨境数据流及适用要求；原件与身份映射受控存储。
- 敏感信息不进入普通日志、前端分析或错误追踪载荷。
- 病历文字属于不可信输入；其中的指令不能改变模型任务、触发工具或越权操作。
- 文档解析隔离执行；校验真实文件类型，限制资源与处理时间。
- 桌面优先，同时支持平板和手机纵向复核；目标WCAG 2.2 AA，正式上线需实测。

## 11. 里程碑与验收

| 里程碑 | 交付 | 验收 |
| --- | --- | --- |
| M1 流程原型 | 合成病例、工作台、编辑审核、导出 | 点击字段可回溯；阻断项控制审核；导出体现修改 |
| M2 真实抽取 | 文档解析、OCR、LLM、证据定位 | 使用批准样本端到端运行；失败可解释、可重试 |
| M3 质量闭环 | 标注指南、金标准、术语映射、评测 | 独立测试集可复现；版本比较有明确指标 |
| M4 机构试点 | 获准环境下的小规模使用 | 满足业务约定质量、成本和审核时间目标 |

性能与质量具体上线阈值需在基线测试及医学风险评估后确定，不以原型中的演示值代替验收。

## 12. HTML 原型验收场景

1. 默认显示合成病例的三栏审核工作台，顶部明确演示模式。
2. 点击检验字段切换到相应报告；证据被高亮。
3. 编辑字段并填写原因，结果和审计记录同步更新。
4. 未处置异常不能提交；剂量可保留未知、编码可保留待映射。
5. 明确勾选已核对原文后方可提交；提交后状态和数据集同步变化。
6. JSON／CSV 包含当前值、来源、审核状态和版本；草稿导出明确标记。
7. 文档任务支持搜索；本地导入仅预览，不伪造抽取结果。
8. 切换模板、术语、数据集和评测页面均有对应内容，演示指标明确标注。
9. 窄屏保留文档、字段和审核操作；键盘可操作，焦点可见。

## 13. 设计参考与来源

产品标识：使用「文档转表格」线性 SVG 图标，采用文档轮廓、文本横线、转换箭头与表格网格。侧栏中文名称分两行排布，保持完整名称与窄屏可读性。

### 前端参考（2026-09-16 查阅）

- [Medplum Chart Demo](https://github.com/medplum/medplum-chart-demo)：临床信息、就诊文书与操作分栏；病历转结构化记录的业务组织。
- [Medplum Charting](https://www.medplum.com/docs/charting)：查阅临床界面示例，参考清晰的患者上下文与工作区划分。
- [Label Studio](https://github.com/HumanSignal/label-studio)：数据标注、预标注、人工修正与标准化输出流程。

本原型独立编写 HTML／CSS／JavaScript；参考信息架构与交互原则，不复制源代码、商标或截图资产。

### 技术与规范来源

- [HL7 FHIR DiagnosticReport](https://hl7.org/fhir/R4/diagnosticreport.html)
- [WHO ICD](https://www.who.int/standards/classifications/classification-of-diseases)
- [LOINC 入门](https://loinc.org/start)
- [MedDRA 访问与许可说明](https://files.meddra.org/www/Website%20Files/Fact%20Sheets/meddra_factsheet1_accessing_meddra-10%20May%202026.pdf)
- [PaddleOCR PP-StructureV3](https://github.com/PaddlePaddle/PaddleOCR/blob/main/docs/version3.x/pipeline_usage/PP-StructureV3.md)
- [中华人民共和国个人信息保护法](https://www.samr.gov.cn/wljys/gzzd/art/2023/art_3ef1e889c1e644d4b65b5f5c7f432386.html)
