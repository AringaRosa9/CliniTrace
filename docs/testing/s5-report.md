# S5 交付与试点工程验收记录

日期：2026-09-20。结论：S5 可在本地完成的开发与合成验证已交付。机构试点尚未放行：FND-05 机构接入、真实质量/容量门槛及签署、现场 UAT/培训/灰度、机构镜像回滚仍待执行；本机 Docker registry 请求 EOF，完整部署镜像构建未完成验证。

## 交付范围

| 工单 | 本次工程交付 | 机构验收状态 |
| --- | --- | --- |
| REL-01 | 既有越权/并发/任务恢复/注入回归；30 组页面/视口 axe 检查；审核工作台扫描；键盘、CSS 200% 缩放；受限 API 负载；依赖漏洞扫描 | 获准真实模型/样本、持续压力、机构安全审查与人工读屏待执行 |
| REL-02 | 非 root 前后端 Dockerfile、standalone、同源网关/生产配置/角色权限；私有监控/告警；维护模式；一致性备份/恢复；项目删除/凭证/历史版本清除；兼容回滚工具 | 镜像构建因 registry EOF 待验证；正式设施、容量/备份监控和镜像回滚演练待接入 |
| REL-03 | 缺项不放行的试点检查：镜像/配置/数据集/资产版本、证据 SHA-256、四方批准、16 类指标/方向/单位/分母、阻断缺陷、留存与联系人 | 未代签质量/高风险/时间/成本门槛，未伪造 UAT 通过或缺陷清零 |
| REL-04 | 独立 OIDC 项目/成员初始化、精确角色授权；UAT/培训/灰度记录与主备联系人、回滚决策手册 | 机构需提供名单/联系人并执行培训和灰度观察 |

工程文件见 [运行手册](../operations/s5.md) 和 [UAT/培训清单](../operations/pilot/uat-training.md)。没有新增业务数据库迁移或更改公开业务 API；`/internal/metrics` 隐藏于业务契约之外，使用独立 token 且网关拒绝转发。

## 本地验证结果

- `bash scripts/ci/check.sh`：后端 Ruff/格式/mypy、前端 ESLint/TypeScript/格式/Vitest、契约生成一致性和 Next.js 生产构建通过；后端 73 项单元/契约通过，29 项设施/演练用例默认跳过；前端 7 项单元通过。
- `RUN_DB_TESTS=1 … pytest backend/tests -q`：101 通过，1 项显式隔离演练跳过，涵盖原 S1～S4 的真实 PostgreSQL RLS、Redis、私有对象存储及 Worker 故障/并发路径。
- `RUN_OPS_DRILL=1 … pytest backend/tests/integration/test_s5_drill.py backend/tests/unit/test_s5_operations.py -q`：13 通过。使用随机合成数据库/桶；自动清理仅限本次创建资源；现有开发数据未被删除。
- `pnpm test:s3`：1 项完整审核/导出/修订失效/并发保留输入通过，增加实际工作台 axe 检查，保留原抽屉 Escape 和焦点恢复断言。
- `pnpm test:s4`：2 项管理/冻结/评测/空态通过；`pnpm test:s5`：2 项通过，含 30 组页面×视口检查与安全/负载；`pnpm test:app`：3 项通过。
- `alembic check`：无新迁移差异；`pnpm audit --audit-level high` 与 `pip-audit --local`：本次查询无已知漏洞。此结果不等于容器操作系统包扫描或渗透认证。
- Compose 配置解析通过；两个 Docker build 都在拉取基础镜像 metadata 时遇到 registry EOF，未进入应用构建，不能记为镜像通过。CI 已增加镜像构建/启动冒烟与本阶段检查，尚未执行远程 CI。
- [未批准清单检查结果](s5/unapproved-gate.json)：ready=false，缺少批准、实测、授权、联系人时正确拒绝。工具只核对证据与声明，不认证签署人身份。

原 S3 浏览器测试曾在创建新导出后、列表更新前点击历史第一条过期文件。已改为按本次唯一用途匹配导出，避免受历史数据影响；服务端 410 过期保护保持不变。检查还修复了旧首页仍把模板/术语/质量标记待开放的问题，六个入口现在均可进入现有功能。

## 恢复、删除与负载证据

- [恢复报告](s5/recovery.json)：45 张表和 12 个对象恢复后一致；校验完整行指纹、原件/派生产物/页图/有效导出引用、审核快照和应用读取。恢复到非空库/桶会被拒绝；篡改 dump/manifest 会被拒绝。
- 删除演练覆盖停写前置、默认预览、保留例外拦截、项目内所有临床台账、对象历史版本及删除标记、另一项目仍可访问。删除意图凭证在删除前 fsync 落盘，完成后原子更新。正式删除仍需机构批准与备份到期/恢复重放流程。
- 本地合成恢复约 0.4 秒，仅反映这一小样本与本机设施，不是机构 RTO；备份频率与异地复制尚无测量，不能推定 RPO。
- [合成 API 负载](s5/load.json)：200 次请求、8 并发，混合文书列表/审核队列/身份读取；P50 86.189ms，P95 104.365ms，失败率 0。工程观察线为 1000ms/0 失败，由测试设定；约 1.8 秒的短时负载不能代表持续压力、并发写审核、模型性能或生产容量。
- 本地只完成维护配置暂停/恢复和恢复副本读取；上一兼容机构镜像、外部 OIDC/TLS、正式模型与机构数据库切换仍需现场演练。

原始浏览器/构建日志保存在 `test-results/s5/`（不提交）；CI 将统一上传回归证据。仓库只保留上述无病例正文的摘要和合成页面截图。

## 界面审查（audit）

反模式检查：通过。维持已有深青绿、暖白文书、紧凑表格与六模块导航，没有引入装饰性卡片、渐变指标或远程字体。

| 维度 | 分数 / 4 | 证据与边界 |
| --- | --- | --- |
| 无障碍 | 3 | 六页面/登录/真实工作台自动扫描无违规；跳转、Escape 与焦点恢复通过；人工读屏仍待 UAT |
| 性能 | 3 | 生产构建与有界 API 负载通过；尚无真实长文书/持续压力数据 |
| 响应式 | 3 | 390/768/1024/1440/1920px 无页面横向溢出，200% CSS zoom 检查通过；仍需真实设备人工试用 |
| 主题 | 3 | 共享 token 保持一致，少量业务 CSS 仍使用字面色值；未承诺深色模式 |
| 反模式 | 4 | 遵循项目既定专业工作台设计，无新增装饰干扰 |
| 合计 | 16 / 20 | Good；这是有边界的工程审查分数，不是 WCAG 认证 |

已修复 P1 1 项：导航辅助字在选中背景下对比度 4.34:1，违反 WCAG 1.4.3；加深共享 muted token 后 axe 通过。已修复 P2 1 项：工作区 main 缺少可靠焦点目标，补充 tabIndex=-1，确保键盘跳转后的焦点位于主内容。没有验证到未关闭的 P0/P1 界面缺陷；人工评估未执行的部分不能认定为通过。

保留 P3 1 项：业务样式仍有少量字面色值（如 `frontend/src/styles/globals.css` 的 hover 背景）；当前未形成可见不一致，未来主题扩展时宜用 `$normalize` 统一，再以 `$polish` 完成最终检查。该事项不阻断本地工程验证。未对机构无障碍结果作保证。

[自动扫描摘要](s5/accessibility.json) · [200% 页面截图](s5/documents-200-percent.png) · [实际审核工作台](s5/review-1440.png)。保留原分栏、证据与密集信息结构；全量人工 WCAG 2.2 AA、辅助设备和机构培训验收列在 UAT-11。
